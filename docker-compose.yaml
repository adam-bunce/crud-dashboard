version: "3.8"
services:
  backend:
    build: ./backend
    container_name: backend_c
    ports:
      - '8080:8080'
  frontend:
    build: ./crud
    container_name: frontend_c
    ports:
      - '3000:3000'
    stdin_open: true